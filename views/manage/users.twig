{% extends "base.twig" %}
{% block main %}
    {% if data is empty %}
        <div class="container mt-4">
            <div class="row text-center">
                <div class="col mb-3">
                    <h1> --- Users --- </h1>
                    <p class='mt-4'>There are no users beside you yet</p>
                </div>
            </div>
        </div>
    {% else %}
        <div class="container mt-4">
            <div class="row text-center">
                <div class="col mb-3">
                    <h1> --- Users --- </h1>
                    {% for user in data %}
                        {{ user.userManage() }}
                    {% endfor %}
                </div>
            </div>
        </div>
    {% endif %}
{% endblock %}