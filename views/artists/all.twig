{% extends "base.twig" %}
{% block main %}
    {% if data is empty %}
        <div class="container mt-4">
            <div class="row text-center">
                <div class="col mb-3">
                    <h1> --- Artists --- </h1>
                    <p class='mt-4'>There are no artists yet</p>
                </div>
            </div>
        </div>
    {% else %}
        <div class="container mt-4">
            <div class="row text-center">
                <div class="col mb-3">
                    <h1> --- Artists --- </h1>
                    {% for artist in data %}
                        {{ artist.artistFormat() }}
                    {% endfor %}
                </div>
            </div>
        </div>
    {% endif %}
{% endblock %}