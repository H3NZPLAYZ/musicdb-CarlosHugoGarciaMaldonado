{% extends "base.twig" %}
{% block main %}
    {% if data is empty and not liked %}
        <div class="container mt-4">
            <div class="row text-center">
                <div class="col mb-3">
                    <h1> --- {{ userName }}'s Lists --- </h1>
                    <p class='mt-4'>There are no lists yet</p>
                </div>
            </div>
        </div>
    {% else %}
        <div class="container mt-4">
            <div class="row text-center">
                <div class="col mb-3">
                    <h1> --- {{ userName }}'s Lists --- </h1>
                    {{ liked.listFormatShow() }}
                    {% for list in data %}
                        {{ list.listFormatShowDel() }}
                    {% endfor %}
                </div>
            </div>
        </div>
    {% endif %}
{% endblock %}